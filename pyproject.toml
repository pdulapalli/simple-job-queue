[project]
name = "simple-job-queue"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.11.5",
    "python-dotenv>=1.1.0",
    "redis[hiredis]>=6.2.0",
]
